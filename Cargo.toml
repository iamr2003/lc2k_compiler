[package]
name = "lc2k_compiler"
version = "0.1.0"
edition = "2021"

# [workspace]
# members = ["src"]

[dependencies]
lalrpop-util = {version = "0.22.0", features = ["lexer"]}

# Add a build-time dependency on the lalrpop library:
[build-dependencies]
lalrpop = "0.22.0"
